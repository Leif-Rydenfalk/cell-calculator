# cell-calculator
Portable calculator cell (add/sub/mul/div).  
Run anywhere with the `cell` CLI:

```bash
cell clone gh:YOUR_GITHUB/cell-calculator@v1
cell start cell-calculator
cell use cell-calculator add '{"a":9,"b":3}'
```